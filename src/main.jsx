import React from "react";
import ReactDOM from "react-dom/client";
import KoreApp from "./components/KoreApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <KoreApp />
  </React.StrictMode>
);
